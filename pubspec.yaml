name: cached_svg_image
description: This using package svg network image cached in reuseable for image
version: 0.0.4
homepage: https://github.com/PanneerDev/cached_svg_image.git

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.3.0
  flutter_svg: ^1.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: